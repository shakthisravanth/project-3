tabs:
  - app/Main.java

run-button: >
  javac numbers/*.java app/*.java &&
  java -cp . app.Main

terminals:
  - javac numbers/*.java app/*.java && java -cp . app.Main

playground-view: terminal-editor
